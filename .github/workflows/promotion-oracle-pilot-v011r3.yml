name: Promotion semantic oracle pilot v0.11r3

on:
  pull_request:
    branches:
      - exp/promotion-oracle-base-v0.11r3

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  trusted-semantic-oracle:
    name: Trusted target-base semantic oracle
    uses: Luminous-Dynamics/symtropy/.github/workflows/promotion-oracle-execution-trusted-v011.yml@81d8eff80111cc7383e31de9fc90963c3cc5af38
    permissions:
      contents: read
      id-token: write
      attestations: write
