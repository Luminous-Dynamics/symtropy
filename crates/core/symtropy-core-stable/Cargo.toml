[package]
name = "symtropy-core-stable"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0 OR MIT"
description = "Stable, independent core for Spacetime Crystallization."

[dependencies]
symtropy-hdc-core = { version = "0.1.0", path = "../symtropy-hdc-core" }
nalgebra = "0.34"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.8"
