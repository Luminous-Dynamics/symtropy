[package]
name = "symtropy-construction"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Evidence-backed construction-site orchestration for Symtropy."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-fabrication = { path = "../symtropy-fabrication" }
symtropy-game-state = { path = "../symtropy-game-state" }

[dev-dependencies]
serde_json = "1.0"
