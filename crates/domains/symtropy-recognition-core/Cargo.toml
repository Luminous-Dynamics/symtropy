[package]
name = "symtropy-recognition-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Source-relative recognition records for Symtropy institutions and office claims."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-civilization-core = { path = "../symtropy-civilization-core" }
symtropy-epistemics-core = { path = "../symtropy-epistemics-core" }
symtropy-game-state = { path = "../symtropy-game-state" }
symtropy-succession-core = { path = "../symtropy-succession-core" }
