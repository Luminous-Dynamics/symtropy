# DEMOG-04B4B1 staging note

B4B1 will introduce the runtime-only validated predecessor token and a shared source-authority gate, then retrofit only the census-resize/bottleneck executor as the first proof-bearing consumer.

This is intentionally narrower than the full #562 rollout. The purpose is to validate the authority pattern before applying it to founder/recolonization, extinction, split, and pulse admixture.

The existing public root-only executor remains unchanged in meaning.
