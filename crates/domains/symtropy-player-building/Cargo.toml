[package]
name = "symtropy-player-building"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Authority-safe player construction intent, planning, constraints, and preview contracts for Symtropy."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
symtropy-game-state = { path = "../symtropy-game-state" }
