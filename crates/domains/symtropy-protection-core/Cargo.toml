[package]
name = "symtropy-protection-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Deterministic generic effect/protection transformation for Symtropy worlds."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
