[package]
name = "symtropy-transfer-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Exactly-once canonical authority transfer for persistent Symtropy subjects."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
