[package]
name = "symtropy-epistemics-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Dependency-light observation, belief, public assertion, and institutional-record semantics for Symtropy."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
