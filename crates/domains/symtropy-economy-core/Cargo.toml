[package]
name = "symtropy-economy-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Causal resource, production, inventory, and shipment flows for Symtropy civilizations."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
