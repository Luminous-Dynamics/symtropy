[package]
name = "symtropy-firstlight-bent-feeder"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Headless Bent Feeder operation kernel for the Firstlight opening."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
symtropy-residents = { path = "../symtropy-residents" }
