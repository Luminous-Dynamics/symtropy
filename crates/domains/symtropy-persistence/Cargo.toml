[package]
name = "symtropy-persistence"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Versioned snapshots and append-only journals for Symtropy gameplay."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
symtropy-game-state = { path = "../symtropy-game-state" }
