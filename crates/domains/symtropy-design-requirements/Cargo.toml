[package]
name = "symtropy-design-requirements"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Requirement traceability and verification-obligation contracts for Symtropy designs."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
symtropy-design = { path = "../symtropy-design" }
symtropy-game-state = { path = "../symtropy-game-state" }
