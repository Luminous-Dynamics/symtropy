[package]
name = "symtropy-succession-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Dependency-light succession policies and competing office claims for Symtropy civilizations."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-civilization-core = { path = "../symtropy-civilization-core" }
symtropy-epistemics-core = { path = "../symtropy-epistemics-core" }
symtropy-game-state = { path = "../symtropy-game-state" }
