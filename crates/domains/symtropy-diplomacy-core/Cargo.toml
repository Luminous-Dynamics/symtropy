[package]
name = "symtropy-diplomacy-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Clause-level treaties, notices, claims, sanctions, and disputes for Symtropy civilization simulation."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
