[package]
name = "symtropy-crawler-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "CLV-7 Wayhouse budgets, systems, cargo, route, and failure state."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
