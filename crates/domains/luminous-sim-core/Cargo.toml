[package]
name = "luminous-sim-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Core traits and types for Luminous Dynamics simulation modules"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
