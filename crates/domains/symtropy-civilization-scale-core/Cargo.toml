[package]
name = "symtropy-civilization-scale-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Identity-safe aggregate/exact civilization projections over external fidelity authority."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
