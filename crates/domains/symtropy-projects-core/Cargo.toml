[package]
name = "symtropy-projects-core"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Open projects, contributions, and evidence-bearing acceptance without implicit membership or payment for Symtropy."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../symtropy-game-state" }
