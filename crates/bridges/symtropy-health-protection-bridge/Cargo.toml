[package]
name = "symtropy-health-protection-bridge"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Deterministic provider-owned mapping from residual world effects to person condition proposals."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
symtropy-game-state = { path = "../../domains/symtropy-game-state" }
symtropy-protection-core = { path = "../../domains/symtropy-protection-core" }
symtropy-residents = { path = "../../domains/symtropy-residents" }
